# Shrinkme Nuclear Bot

بوت لفتح رابط shrinkme.ink تلقائيًا مع تخطي الروابط وحل الكابتشا باستخدام 2captcha.

## طريقة التشغيل

1. ثبت المتطلبات:
   ```bash
   pip install -r requirements.txt
   playwright install
